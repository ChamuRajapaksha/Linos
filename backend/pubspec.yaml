name: linos_backend
description: "Linos backend proxy — Dart Shelf server with SQLite caching and UG scraping."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.11.0

dependencies:
  html: ^0.15.0
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  sqlite3: ^3.5.2

dev_dependencies:
  http: ^1.2.0
  test: ^1.25.0
